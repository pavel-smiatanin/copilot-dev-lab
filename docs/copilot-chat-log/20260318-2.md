### Prompt
Analyze #file:srs.md and implement task 15. Then mark task 15 as done in #file:backlog.md

### Result
Implemented the frontend MVP create-link flow (backlog task 15). Analysis revealed several bugs and missing features in the existing partial implementation.

**Files modified:**

`frontend/src/types/linkTypes.ts`
- Fixed `CreateLinkResponse` to match actual backend shape: removed non-existent metadata fields (`title`, `ogTitle`, `ogImageUrl`, `faviconUrl`), added `destinationUrl` and `hasPassword`
- Removed `ConflictAlternative` (backend sends suggestions as plain strings, not objects)
- Renamed `CreateLinkConflictResponse.alternatives` → `suggestions: readonly string[]` to match backend 409 response
- Added `ValidationError` and `ValidationErrorResponse` interfaces to model the backend 400 response format (`{ errors: [{ field, message }] }`)

`frontend/src/pages/HomePage.tsx`
- Added `FieldErrors` interface and `fieldErrors` state for both client-side and server-side field-level error display
- Added `validateForm()` function: validates destination URL (required, must be http/https scheme), validates alias format against `ALIAS_PATTERN`
- `handleSubmit` now runs client-side validation and aborts submission with field errors shown if invalid
- Fixed 409 handler: now reads `body.suggestions` (array of strings) instead of `body.alternatives`; `conflictAlternatives` state renamed `conflictSuggestions` and typed `readonly string[]`
- Added 400 server error parsing: maps `{ field, message }` entries back to `fieldErrors` for inline display
- `handleChange` now clears the corresponding field error on each keystroke, and clears conflict suggestions when the alias field changes
- Inputs show `inputError` CSS class and `aria-describedby` wired to error `<p>` elements when errors are present
- Separated 429 and 500 error display (were previously collapsed into one `isGenericError` path)
- Result block: added "View stats" `<Link>` pointing to `/stats/${result.id}`; "Create another" button also clears `fieldErrors`

`frontend/src/pages/HomePage.module.css`
- Added `inputError` class: red border + red focus outline for error-state inputs
- Added `resultActions` flex row class for the result section action buttons
- Added `statsLink` and `statsLink:hover` styles

`docs/backlog.md`
- Marked task 15 as `[x]`

-------------
