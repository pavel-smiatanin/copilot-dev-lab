### Prompt
1. analyze requirements in #file:srs.md and implement task 10; 2. save interaction in file according to instructions 3. mark task 10 as done

### Result
Implemented task 10: **password-protected link flow** (`POST /api/v1/links/{alias}/unlock` + token-based redirect completion via `GET /{alias}?token=<value>`).

**New files created:**
- `UrlShortener.Application.Abstract/Secondary/IUnlockTokenService.cs` — secondary port interface for token issuance and validation
- `UrlShortener.Application.Abstract/Primary/Commands/UnlockLinkCommand.cs` — MediatR command (alias + password)
- `UrlShortener.Application.Abstract/Primary/Commands/UnlockLinkResult.cs` — discriminated union result: `Success(Token)`, `NotFound`, `InvalidPassword`
- `UrlShortener.Application/Commands/UnlockLinkCommandHandler.cs` — bcrypt password verification, HMAC token issuance; distinguishes "alias not found" vs "no password" via anonymous projection
- `UrlShortener.Application/Commands/UnlockLinkCommandValidator.cs` — FluentValidation: alias pattern + password required
- `UrlShortener.Adapter.BackingServices/Security/HmacUnlockTokenService.cs` — HMAC-SHA-256 signed token (5-min lifetime); base64url payload `{alias}:{expiryEpochSeconds}`; constant-time signature comparison
- `UrlShortener.Adapter.Api/Model/UnlockLinkRequest.cs` — request body (Password only)
- `UrlShortener.Adapter.Api/Model/UnlockLinkResponse.cs` — response body (Token)

**Modified files:**
- `GetLinkByAliasQuery.cs` — added `string? Token` parameter
- `GetLinkByAliasQueryHandler.cs` — injected `IUnlockTokenService`; token validated in both cache-hit and DB-miss paths; valid token bypasses unlock gate and returns redirect
- `LinksController.cs` — added `POST /api/v1/links/{alias}/unlock` endpoint; maps result to 200/400/404
- `RedirectController.cs` — added `[FromQuery] string? token` param, passed to query
- `ServiceCollectionExtensions.cs` — registered `HmacUnlockTokenService` as singleton with secret from config
- `appsettings.json` — added `UnlockToken.HmacSecret` placeholder
- `appsettings.localdev.json` — added localdev HMAC secret value
- `GetLinkByAliasQueryHandlerTests.cs` — updated `CreateHandler` factory to inject `IUnlockTokenService` mock; all `GetLinkByAliasQuery` usages updated to include `null` token

Build succeeded: 0 errors, 0 warnings.

-------------
