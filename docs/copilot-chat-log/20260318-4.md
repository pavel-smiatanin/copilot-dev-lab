### Prompt
analyze #file:srs.md and implement task 16 from #file:backlog.md. Then mark task 16 as done

### Result
Analyzed the SRS (flows 4.4 password-protected link access and 4.5 view link stats) and verified the implementation of backlog task 16: "Build frontend MVP unlock and stats flows with React Query + typed Axios service layer and explicit loading/error/success states".

Upon inspection, all required pieces were already fully implemented in a prior session:

**UnlockPage** (`src/pages/UnlockPage.tsx` + `UnlockPage.module.css`):
- `useMutation` from React Query for the `POST /api/v1/links/{alias}/unlock` call
- Explicit loading state (`isPending`) disables the submit button
- Error states: `401` → "Incorrect password", `429` → rate-limit message with `Retry-After` header parsing, generic 500 fallback
- On success, navigates to `/{alias}?token=<token>` to complete the redirect (FR-PASS-3)
- Accessible: `aria-required`, `aria-describedby` wired to error container, `autoFocus`, `autoComplete`

**StatsPage** (`src/pages/StatsPage.tsx` + `StatsPage.module.css`):
- `useQuery` from React Query for `GET /api/v1/links/{id}/stats`
- Loading state: "Loading statistics…" text
- Error states: `404` → "Link not found or has expired", generic fallback
- Success: summary cards (total visits, unique visitors), visits-by-day bar chart, top referrers list
- `staleTime: 60_000` to avoid over-fetching

**Services / Types**: `linksService.ts` exports `getLinkStats` and `unlockLink`; `linkTypes.ts` defines `UnlockRequest`, `UnlockResponse`, `LinkStatsResponse`, `DailyVisit`, `TopReferrer` interfaces. All routes wired in `App.tsx`.

Marked backlog task 16 as `[x]` done.

-------------
