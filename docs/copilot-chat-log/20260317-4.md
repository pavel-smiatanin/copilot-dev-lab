### Prompt
analyze requirements in #file:srs.md and implement task 8 in #file:backlog.md then create interaction log file as pointed in instructions and mark task 8 as done in backlog.md

### Result
Implemented task 8: redirect flow (`GET /{alias}`) with cache-first lookup, DB fallback, cache repopulation, expiry handling, and 302/404 behavior.

**New files created:**

- `backend/src/UrlShortener.Application.Abstract/Primary/Queries/GetLinkByAliasQuery.cs` — MediatR query record `GetLinkByAliasQuery(string Alias) : IRequest<GetLinkByAliasResult>`
- `backend/src/UrlShortener.Application.Abstract/Primary/Queries/GetLinkByAliasResult.cs` — Discriminated union result with three sealed subtypes: `NotFound`, `Redirect(string DestinationUrl)`, `RequiresUnlock`
- `backend/src/UrlShortener.Application/Links/Queries/CachedLinkData.cs` — Internal sealed record serialized into Redis under `alias:{shortId}` key; fields: `LinkId`, `DestinationUrl`, `ExpiresAt`, `HasPassword`
- `backend/src/UrlShortener.Application/Links/Queries/GetLinkByAliasQueryHandler.cs` — Handler implementing cache-first lookup, Redis fallback-on-error, DB lookup, cache repopulation with TTL = `Min(expiresAt - now, 24h)`, expiry detection, and password detection
- `backend/src/UrlShortener.Application/AssemblyAttributes.cs` — `InternalsVisibleTo("UrlShortener.Application.Tests")` for test access to internal `CachedLinkData`
- `backend/src/UrlShortener.Adapter.Api/Controllers/RedirectController.cs` — `[Route("{alias}")]` controller; `GET /{alias}` → 302 redirect, 200 unlock signal, or 404
- `backend/src/UrlShortener.Adapter.Api/Model/RequiresUnlockResponse.cs` — Response record `(bool RequiresUnlock, string Alias)` returned with 200 for password-protected links
- `backend/tests/UrlShortener.Application.Tests/Links/Queries/GetLinkByAliasQueryHandlerTests.cs` — 11 unit tests covering cache hit/miss scenarios, expiry handling, password protection, cache population, and Redis graceful degradation

**Files modified:**

- `backend/src/UrlShortener.Application/UrlShortener.Application.csproj` — Added `Microsoft.Extensions.Caching.Abstractions 8.0.0` package reference
- `backend/src/UrlShortener.Application.Abstract/UrlShortener.Application.Abstract.csproj` — Removed empty `<Folder Include="Primary\Queries\" />` entry
- `docs/backlog.md` — Marked task 8 as done `[x]`

**Key design decisions:**
- Cache key normalized to lowercase (`alias:{alias.ToLowerInvariant()}`) to handle case-insensitive DB collation consistently
- Redis unavailability is caught and logged at Warning level; alias resolution falls back to DB (NFR-REL-2)
- Expired links loaded from DB are not written to cache (TTL guard: if TTL ≤ 0 skip cache write)
- Password-protected links return `200 { requiresUnlock: true, alias }` so the SPA can render the unlock form (FR-PASS-1; full unlock flow deferred to task 10)
- Visit recording (FR-REDIR-4) not yet implemented — deferred to task 9

**Test results:** All tests pass (build exit 0, test exit 0).

-------------
